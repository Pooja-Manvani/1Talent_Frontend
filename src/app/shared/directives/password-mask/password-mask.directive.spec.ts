import { PasswordMaskDirective } from './password-mask.directive';

describe('PasswordMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordMaskDirective();
    expect(directive).toBeTruthy();
  });
});
